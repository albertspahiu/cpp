#include <iostream> 
using namespace std;

int main()
{

  int x;

	cout << "Vlera x: ";
	cin >> x;
	cout << endl;

	if (x > 0)
	cout << "x eshte pozitiv" << endl;

	else if (x < 0)
	cout << "x eshte negativ" << endl;

	return 0;
}
